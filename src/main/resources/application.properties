spring.application.name=vlibrary

# Default profile is 'dev'
spring.profiles.active=dev

# JWT secret and Google API key must be set via environment variables in production
app.jwt.secret=${JWT_SECRET:dev-secret-key}
google.api.key=${GOOGLE_API_KEY:dev-google-api-key}
